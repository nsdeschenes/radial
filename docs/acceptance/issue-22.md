# Issue 22 acceptance evidence

| Promise                                                                                                                                                                    | Automated evidence                                                                                                                                                   | Implementation evidence                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NDB candidates are admitted only after the complete permitted VOR-family search fails.                                                                                     | `keeps the completed VOR-family Route Plan when an NDB Route Plan would be shorter`; `returns a successful NDB-fallback Route Plan only after VOR-family exhaustion` | `src/route-planner/RoutePlanner.ts` runs the NDB discovery session only when the VOR-family graph has no selected route after its discovery loop completes.                               |
| Eligible NDBs have a stable database ID, identifier, finite coordinates, positive published range, and finite positive kHz frequency data; other families remain excluded. | `returns a successful NDB-fallback Route Plan only after VOR-family exhaustion`; `exhausts the mixed graph after excluding ineligible facilities`                    | `src/route-planner/internal/duckdb/repository.ts` applies the NDB eligibility filter and maps admitted rows to `NdbRoutePoint`; the existing VOR-family allowlist remains unchanged.      |
| Fallback progressively adds NDB candidates to the existing monotonic graph without changing Navigable Route Leg rules.                                                     | The NDB-only scenario places both candidates outside the initial 1.10D ellipse; the route-search monotonic-graph and coverage suites continue to pass.               | `src/route-planner/RoutePlanner.ts` reuses the discovery session and graph; `src/route-planner/internal/routeSearch.ts` applies the existing coverage predicates to both Navaid families. |
| A successful VOR-family result wins even when an available NDB Route Plan is shorter, and adding NDB rows cannot alter it.                                                 | `keeps the completed VOR-family Route Plan when an NDB Route Plan would be shorter` compares results with and without the shorter NDB option.                        | The fallback branch in `src/route-planner/RoutePlanner.ts` is unreachable once the VOR-family phase selects a Route Plan.                                                                 |
| A mixed-family Route Plan is returned deterministically only after VOR-family exhaustion and successful mixed-graph search.                                                | `selects the same stable mixed-family Route Plan for ten database row permutations`                                                                                  | `src/route-planner/internal/routeSearch.ts` retains exact-distance, Route Leg count, identifier, and database-ID ordering for the widened Navaid union.                                   |
| Fallback success reports NDB-fallback Route Search Mode and the structured fallback warning while remaining successful.                                                    | `returns a successful NDB-fallback Route Plan only after VOR-family exhaustion`                                                                                      | `src/route-planner/RoutePlanner.ts`; `src/route-planner/internal/warnings.ts`                                                                                                             |
| Exhausting the complete mixed graph returns normalized endpoints and completed limits in a structured no-route result.                                                     | `exhausts the mixed graph after excluding ineligible facilities` verifies `AAAA`, `BBBB`, and `[1.1, 1.25, 1.5]`.                                                    | `src/route-planner/RoutePlanner.ts` returns no-route only after both discovery sessions complete without a selected route.                                                                |
| Repository checks and tests pass.                                                                                                                                          | `nub run check`; `nub run test`                                                                                                                                      | CI/release check                                                                                                                                                                          |
