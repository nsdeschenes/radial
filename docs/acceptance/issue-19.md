# Issue 19 acceptance evidence

| Promise                                                                                                                                                                                                                                      | Automated evidence                                                                                                                                                                     | Implementation evidence                                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| A fresh planner-ready DuckDB database can be created from declarative airport, Navaid, and metadata definitions.                                                                                                                             | `plans a complete two-leg Route Plan using a VOR-family Navaid from a fresh synthetic database`                                                                                        | `src/test/route-planner/createSyntheticPlannerDatabase.ts`                                                                               |
| Opening loads and probes DuckDB Spatial, explicitly preserves longitude/latitude point storage, and validates the three planner-ready views.                                                                                                 | `rejects geometry whose point ordinates are not longitude/latitude`; `rejects a missing required planner-ready column before route search`                                             | `src/route-planner/RoutePlanner.ts`; `src/route-planner/internal/duckdb/contract.ts`                                                     |
| Missing or duplicate metadata, missing or incompatible columns, invalid geometry, and partial magnetic metadata fail before search as `database-contract-invalid`.                                                                           | The missing/duplicate/partial metadata, missing/wrong-type column, swapped point, and non-finite Navaid geometry contract tests                                                        | `src/route-planner/internal/duckdb/contract.ts`                                                                                          |
| Airport lookup uses normalized exact ICAO equality and distinguishes zero, one, and multiple usable records.                                                                                                                                 | The complete Route Plan test; `returns a structured failure for missing departure`; `returns a structured failure for ambiguous normalized departure`                                  | `src/route-planner/internal/duckdb/repository.ts`; `src/route-planner/RoutePlanner.ts`                                                   |
| Only the six eligible VOR-family types with usable identity, MHz frequency, coordinates, and positive range are admitted; DME and TACAN are excluded.                                                                                        | `admits an eligible … facility into a Route Plan using VOR-family Navaids` (six cases); `filters ineligible facilities and never creates an airport-to-airport Route Leg`              | `src/route-planner/internal/duckdb/repository.ts`                                                                                        |
| DuckDB Spatial supplies spherical great-circle metres, converted with exactly 1,852 metres per nautical mile.                                                                                                                                | The complete structured-result test asserts both full-precision leg distances; the exact-boundary integration cases assert independently calculated one-degree distances.              | `src/route-planner/internal/duckdb/repository.ts`                                                                                        |
| An airport–VOR-family Navaid Route Leg is navigable at and inside published coverage, and no airport-to-airport Route Leg is created.                                                                                                        | The pure and public-seam inclusive published-coverage-boundary tests; the ineligible-facility no-route test                                                                            | `src/route-planner/internal/coverage.ts`; `src/route-planner/RoutePlanner.ts`                                                            |
| A departure–VOR-family Navaid–arrival request returns the approved complete structured Route Points, Route Legs, full-precision total, VOR-family-only Route Search Mode, magnetic values, VOR Guidance, and distinct endpoint true courses. | The complete Route Plan test deep-compares the entire public result.                                                                                                                   | `src/route-planner/internal/navigation.ts`; `src/route-planner/RoutePlanner.ts`                                                          |
| Independent course examples, antimeridian normalization, eligibility, boundaries, and public structured results have pure/public-seam evidence.                                                                                              | `calculates independently worked endpoint courses without rounding`; `normalizes endpoint courses across the antimeridian`; the eligibility, coverage, and complete-result cases above | `src/route-planner/internal/navigation.test.ts`; `src/route-planner/internal/coverage.test.ts`; `src/route-planner/RoutePlanner.test.ts` |
| Repository checks and tests pass.                                                                                                                                                                                                            | `nub run check`; `nub run test`                                                                                                                                                        | CI/release check                                                                                                                         |
