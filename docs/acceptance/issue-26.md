# Issue 26 acceptance evidence

| Promise                                                                    | Evidence                                                                                    | Implementation evidence                                                                                            |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| Every public promise has named evidence.                                   | `route-planner-evidence-matrix.md` traces all 60 issue #4 user stories.                     | The matrix names blocking automated evidence and five release checks.                                              |
| Deterministic evidence blocks CI; coverage remains diagnostic.             | `nub run test:acceptance`; `nub run test`; diagnostic coverage job                          | `.github/workflows/ci.yml`; no coverage threshold is configured.                                                   |
| A deterministic synthetic stress corpus is available.                      | `keeps the committed synthetic stress corpus Route Plan deterministic`                      | `src/acceptance/syntheticStressCorpus.ts` fixes two airports and 148 candidates.                                   |
| Smoke refuses a mismatched real-data snapshot before planning.             | `refuses a snapshot checksum mismatch before opening the planner`                           | `verifyAcceptanceSnapshot.ts` runs before DuckDB inspection or planner opening.                                    |
| Smoke validates the qualifying Route Plan and every output invariant.      | `verifies the pinned Route Plan and byte-identical CLI output`; `RP-REAL-SMOKE`             | `routePlannerSmoke.ts` checks snapshot facts, structured Route Plan invariants, coverage, cap, and two CLI runs.   |
| Benchmark uses an open planner, one warm-up, and five fresh-session calls. | `warms one open planner and records five fresh-session planning calls`; `RP-WARM-BENCHMARK` | `routePlannerBenchmark.ts` opens once and invokes the public planner six times sequentially.                       |
| Only the representative-machine median has an under-two-second gate.       | Benchmark acceptance suite; `RP-WARM-BENCHMARK`                                             | The report gives all samples, median, worst, machine/runtime metadata, and a nullable representative-machine gate. |
| Smoke and benchmark use one qualifying route.                              | Both commands accept the same runtime-validated baseline and verify its ordered identities. | `readAcceptanceBaseline.ts` is their shared authoritative baseline reader.                                         |
| Release and baseline updates are repeatable without duplicated values.     | `RP-SNAPSHOT-PIN`; `RP-RELEASE-REVIEW`                                                      | `route-planner-release.md` references the structured baseline instead of copying its values.                       |
| Repository checks and tests pass.                                          | `nub run check`; `nub run test`                                                             | Blocking CI/release checks                                                                                         |
