# Issue 25 acceptance evidence

| Promise                                                                                                                                                                                      | Automated evidence                                                                                                                                                                                               | Implementation evidence                                                                                                                                       |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Successful output begins with the ordered Route Point sequence, total distance, Route Leg count, and human-readable Route Search Mode.                                                       | `renders the complete Route Plan with display-only rounding and calculated alignment`; `writes a complete normal Route Plan to stdout and exits 0`                                                               | `src/cli/formatRoutePlan.ts`; `src/cli/runCli.ts`                                                                                                             |
| Long Route Point sequences wrap deterministically at 100 characters only between Route Points, with aligned continuation indentation.                                                        | `wraps long Route Point sequences at 100 characters only between points`                                                                                                                                         | `src/cli/formatRoutePlan.ts`                                                                                                                                  |
| The Route Leg table presents endpoint identifiers, distance, distinct endpoint true and magnetic courses, and endpoint-specific VOR Guidance.                                                | The complete formatter and normal CLI multiline tests                                                                                                                                                            | `src/cli/formatRoutePlan.ts` renders only values already carried by each structured Route Leg.                                                                |
| The Navaid table contains every used Navaid once with exact family/type, conventional frequency unit, and published range.                                                                   | `renders each used Navaid once with its exact type and conventional frequency unit`; the normal and degraded CLI multiline tests                                                                                 | `src/cli/formatRoutePlan.ts` deduplicates by stable database ID.                                                                                              |
| Table widths derive from headers and rendered cells, columns use two spaces, numeric/text alignment is applied, and cells are not truncated or wrapped.                                      | `renders the complete Route Plan with display-only rounding and calculated alignment`; the long Unicode Navaid case                                                                                              | `src/cli/formatRoutePlan.ts`                                                                                                                                  |
| Unavailable values use an em dash and display values follow the approved distance, course, VOR-family frequency, NDB frequency, and total rounding rules without altering planner precision. | The complete, Unicode/NDB, and degraded formatter/CLI multiline tests                                                                                                                                            | `src/cli/formatRoutePlan.ts` formats the immutable structured Route Plan without feeding values back into planning.                                           |
| Normal and degraded successes write the Route Plan to stdout, summarize warnings on stderr, emit grouped warning details when `--warnings` is requested, and exit 0.                         | `writes a complete normal Route Plan to stdout and exits 0`; `summarizes degraded Route Plan warnings unless details are requested`; `renders ordered warnings separately with their Route Leg endpoint context` | `src/cli/runCli.ts`; `src/cli/formatRoutePlanningWarningSummary.ts`; `src/cli/formatRoutePlanningWarnings.ts`                                                 |
| Invalid request, initialization, lookup, ambiguity, query/search, database-contract, and no-route outcomes use deterministic diagnostics and statuses without partial output.                | All failure and usage cases in `src/cli/runCli.test.ts`, including real synthetic-database lookup, ambiguity, and exhaustive no-route cases                                                                      | `src/cli/formatDiagnostics.ts`; `src/cli/runCli.ts`                                                                                                           |
| The CLI remains a thin adapter and performs no database query or navigation calculation.                                                                                                     | The exact public CLI tests exercise the application/planner boundary; Route Planner integration tests retain navigation evidence.                                                                                | `src/cli/runCli.ts` only parses configuration/arguments, invokes and disposes the application/planner, formats outcomes, writes channels, and returns status. |
| Repository checks and tests pass.                                                                                                                                                            | `nub run check`; `nub run test`                                                                                                                                                                                  | CI/release check                                                                                                                                              |
