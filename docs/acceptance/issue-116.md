# Issue 116 acceptance evidence

| Promise                                                                                                                                                                                                                     | Deterministic evidence                                                                                                        | Implementation evidence                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| The executable assigns final statuses, rethrows defects, bridges the first signal, returns silent 130, restores default second-signal behavior, and preserves late committed success.                                       | `src/cli/runCliExecutable.test.ts` drives the real root-help composition and the shared executable bootstrap in subprocesses. | `src/cli/runCliExecutable.ts`; `src/cli/runtime/createCliSignalBridge.ts`; `src/radial.ts` |
| The lazy application scope handles interruption before and during open, suppresses callbacks, reuses one configuration, rejects competing configurations, and disposes exactly once with cleanup defects taking precedence. | `src/cli/runtime/createCliRuntimeContext.test.ts`; the cleanup and late-result cases in `src/cli/runCli.test.ts`              | `src/cli/runtime/createCliRuntimeContext.ts`; framework-neutral command handlers           |
| Cold and admitted telemetry retain stable metadata, ordering, binary outcomes, exactly-once defect capture, bounded flush, and mandatory close attempts.                                                                    | `src/cli/runCliLifecycle.test.ts`; `src/cli/telemetry/createCliTelemetrySession.test.ts`                                      | `src/cli/runAdmittedCliCommand.ts`; `src/cli/telemetry/createCliTelemetrySession.ts`       |
| Stricli 1.3.0 is the sole parser authority, eager modules remain import-light, and CLI modules use direct descriptive imports without barrels or forwarding modules.                                                        | `src/cli/stricliArchitecture.test.ts`; `src/cli/publicCliCompatibility.test.ts`                                               | `src/cli/runCli.ts`; `package.json`                                                        |
| The Route Plan CLI bytes remain compatible.                                                                                                                                                                                 | `src/acceptance/routePlannerSmoke.test.ts`; `nub run acceptance:smoke` against the configured pinned real-data baseline       | `src/acceptance/routePlannerSmoke.ts`; `fixtures/acceptance/route-planner-baseline.json`   |
| Every repository gate passes.                                                                                                                                                                                               | `nub run check`; focused CLI suites; `nub run test`; `nub run acceptance:smoke`                                               | CI and the Route Plan release checklist                                                    |

The structural review also checks that there is no legacy dispatcher, raw-argument
telemetry classifier, parser migration flag, fallback or shadow parser, hidden-route
alias, or compatibility forwarding module. Expected raw-argument inspection remains
confined to compatibility diagnostics after Stricli rejects an invocation.
