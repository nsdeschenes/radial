# Issue 24 acceptance evidence

| Promise                                                                                                                           | Automated evidence                                                                                                                                            | Implementation evidence                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Opening creates and validates one DuckDB instance for the planner's immutable configuration.                                      | `plans a complete two-leg Route Plan using a VOR-family Navaid from a fresh synthetic database`; the Route Planner contract suite                             | `src/route-planner/RoutePlanner.ts` creates the instance once, loads and probes Spatial, validates the planner-ready contract, and retains that instance for the opened planner.                                        |
| Every planning call uses an independent read connection and transaction that observes one committed snapshot.                     | `each planning call observes one committed database snapshot` verifies pre-commit and post-commit observations through the public planner boundary.           | `src/route-planner/internal/duckdb/repository.ts` connects and begins a read transaction for each call, then commits and closes that call's connection.                                                                 |
| Concurrent calls have independent request-local graph, discovery, and result state.                                               | `returns independent structured results from concurrent planning calls` plans two different routes concurrently and verifies each ordered Route Point result. | `src/route-planner/RoutePlanner.ts` creates the graph, discovery sessions, candidate identities, Route Points, and Route Legs inside each `planRoute` operation.                                                        |
| Database query failures remain structured and never expose parsed exception prose.                                                | `returns database query failures as structured planning failures` removes a required planner view after opening and verifies the exact structured failure.    | `src/route-planner/RoutePlanner.ts` maps failed query phases to stable operation names without exposing DuckDB driver messages.                                                                                         |
| Starting disposal rejects new planning calls and waits for every already-active call to settle before closing the owned instance. | `disposal drains active planning and deterministically rejects later calls` verifies rejection while closing and planning-before-disposal settlement order.   | `src/route-planner/RoutePlanner.ts` changes state to `closing` synchronously, snapshots all active promises, waits with `Promise.allSettled`, and only then closes the instance.                                        |
| Planning after disposal is rejected without touching or reopening DuckDB.                                                         | `disposal drains active planning and deterministically rejects later calls` invokes `planRoute` again after disposal.                                         | The lifecycle guard runs synchronously before request validation or repository access. Closing and disposed calls share the documented rejection: `Cannot plan a route while the Route Planner is closing or disposed.` |
| Repeated disposal is idempotent and returns the first disposal operation.                                                         | `disposal drains active planning and deterministically rejects later calls` disposes the planner twice successfully.                                          | `src/route-planner/RoutePlanner.ts` memoizes one disposal promise, so close and drain run once.                                                                                                                         |
| Repository checks and tests pass.                                                                                                 | `nub run check`; `nub run test`                                                                                                                               | CI/release check                                                                                                                                                                                                        |
