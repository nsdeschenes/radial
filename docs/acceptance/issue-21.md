# Issue 21 acceptance evidence

| Promise                                                                                                                                                | Automated evidence                                                                                                                                                                                                                                                               | Implementation evidence                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Candidate discovery widens through monotonic endpoint-ellipse limits of 1.10D, 1.25D, and 1.50D, bounded by a smaller configured factor.               | `widens through the scheduled endpoint ellipses and stops at the configured or VOR-family ceiling`; the public candidate-discovery cases cover each inclusive scheduled boundary.                                                                                                | `src/route-planner/internal/progressiveVorFamilyDiscovery.ts`; `src/route-planner/RoutePlanner.ts`                                                            |
| DuckDB Spatial envelopes are conservative prefilters, while exact canonical endpoint-distance sums own admission.                                      | `admits only candidates newly inside the inclusive endpoint ellipse`; `discovers a VOR-family candidate at the inclusive 1.50 boundary`; `discovers a VOR-family candidate at the immediately outside the completed 1.50 ellipse`                                                | `src/route-planner/internal/duckdb/repository.ts` uses constant `ST_Intersects` envelopes before `ST_Distance_Sphere` admission.                              |
| Conservative candidate discovery remains correct across the antimeridian and near a pole.                                                              | `splits conservative endpoint bounds at the antimeridian and spans all longitude near a pole`; `discovers a VOR-family candidate across the antimeridian`                                                                                                                        | `src/route-planner/internal/progressiveVorFamilyDiscovery.ts`                                                                                                 |
| Each widening returns only newly admitted candidates and compares every new VOR-family Navaid with admitted VOR-family Navaids once.                   | `measures each candidate once and retains its canonical distances until admission`; `retains one monotonic graph while newly admitted candidates improve a provisional Route Plan` rejects duplicate candidates and unordered candidate-pair comparisons.                        | `src/route-planner/internal/progressiveVorFamilyDiscovery.ts`; `src/route-planner/internal/duckdb/repository.ts`; `src/route-planner/internal/routeSearch.ts` |
| The graph remains monotonic and sparse while the accepted initial pairing strategy is quadratic.                                                       | `retains one monotonic graph while newly admitted candidates improve a provisional Route Plan`; issue 20's exhaustive and deterministic route-search suites continue to pass.                                                                                                    | The repository enumerates new-to-admitted pairs; the graph retains candidates and only Navigable Route Leg adjacency rather than a dense distance graph.      |
| A first Route Plan is provisional until discovery completes the smaller of its length and 1.50D, and a shorter later Route Plan replaces it.           | `completes through an improving provisional Route Plan before returning it`; `replaces an early provisional Route Plan after completing its improving ellipse`                                                                                                                   | `src/route-planner/internal/progressiveVorFamilyDiscovery.ts`; `src/route-planner/RoutePlanner.ts`                                                            |
| A candidate exactly on the inclusive 1.50D boundary is admitted, while an irrelevant candidate outside the completed ellipse cannot affect the result. | `discovers a VOR-family candidate at the inclusive 1.50 boundary`; `discovers a VOR-family candidate at the immediately outside the completed 1.50 ellipse`; `returns the same continuous multi-Navaid Route Plan for ten database row permutations and an irrelevant candidate` | `src/route-planner/internal/duckdb/repository.ts`                                                                                                             |
| Exhausting the complete VOR-family region produces an explicit conclusive state for the fallback phase.                                                | `filters ineligible facilities and never creates an airport-to-airport Route Leg` verifies exhaustive `completedSearchLimits` of `[1.1, 1.25, 1.5]`.                                                                                                                             | `src/route-planner/RoutePlanner.ts` represents the internal outcome as `status: 'exhausted'` before mapping it to the current public `no-route` result.       |
| Repository checks and tests pass.                                                                                                                      | `nub run check`; `nub run test`                                                                                                                                                                                                                                                  | CI/release check                                                                                                                                              |
